# go-resturant-management-backend
This is the backend of Resturant Management System in Go.
Full CRUD functionality is implemented in Golang including JWT authentication. It uses Gin for routing and MongoDB for storing the data. 
